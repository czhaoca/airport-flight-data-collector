{
  "name": "airport-flight-data-collector",
  "version": "1.0.0",
  "description": "Cloudflare Worker to collect flight data from multiple airports",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "node deploy.js"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}